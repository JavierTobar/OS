#ifndef INTERPRETER_H
#define INTERPRETER_H

/*
  This function calls tokenize on the user's input 
  Tokenize will sepererate the user's input into tokens 
  interpret will then analyze these tokens and take care of them
*/
int interpret(char *raw_input);

#endif
